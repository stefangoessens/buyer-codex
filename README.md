# buyer-codex

This repo no longer contains the broken Linear/Codex automation runner.

What remains here is the minimal bootstrap currently tracked in git:

- `convex/` generated Convex scaffold
- package metadata
- project instructions in `AGENTS.md`

If automation is reintroduced later, it should live in a separate dedicated repo with explicit repo scoping and no shared worktree root.
