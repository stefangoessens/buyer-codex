/**
 * Programmatic theme access for web surfaces.
 *
 * Canonical token definitions live in @buyer-codex/shared/theme so docs, web,
 * and the generated SwiftUI mirror can all read from the same contract.
 */
export {
  breakpoints,
  colors,
  layout,
  motion,
  radii,
  semanticColors,
  semanticMotion,
  semanticRadii,
  semanticShadows,
  semanticSpacing,
  semanticTypography,
  shadows,
  spacing,
  themeTokens,
  tokenUsage,
  typography,
} from "@buyer-codex/shared/theme";
